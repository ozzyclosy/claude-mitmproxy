meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
  useCache: false
run:
  scriptName: null
  command: mitmproxy -s mitmproxy_proxy.py --listen-port 80 --ssl-insecure
  persistenceMount: /data
  containerPort: "80"
