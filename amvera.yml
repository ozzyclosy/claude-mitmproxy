meta:
  environment: python
  toolchain:
    name: pip
    version: 3.11
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  persistenceMount: /data
  command: python -m mitmproxy --host 0.0.0.0 --port 80 mitmproxy:app --ssl-insecure
  containerPort: 80
serviceType: compute
